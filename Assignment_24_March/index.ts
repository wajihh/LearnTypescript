// Favorite movie with string type annotation
let favoriteMovie: string = "Sultan Abdul Hameed";
console.log(favoriteMovie)

// Template string for introduction
const introduction: string = `Asslam o alikum, I'm Wajih and I'm 66 years old.`;
console.log(introduction)

// Current mood with boolean type annotation (true/false)
const isHappy: boolean = true;  // You can change this to reflect your mood
console.log(isHappy)

// Combine first and last name using template literal
const firstName: string = "Wajih";
const lastName: string = "Hashmi";
const fullName: string = `${firstName} ${lastName}`;
console.log(`Full Name: ${fullName}`);

// User age with number type annotation
let userAge: number = 66;
console.log(userAge)

// Product name and price with template string
const productName: string = "Laptop";
const productPrice: number = 100000;
console.log(`${productName}: Rs${productPrice}`);

// Number of pets with number type annotation
let numPets: number = 0;
console.log(numPets)
// Birthday boolean
let isBirthday: boolean = false; // Change this to true if it's your birthday!
console.log(isBirthday)

// User info with template string
const userInfo: string = `User: ${firstName} ${lastName}, Age: ${userAge}`;
console.log(userInfo);

// Birth year constant with number type annotation
const birthYear: number = 1957;
console.log(birthYear)
// Address using template string
const city: string = "Lahore";
const country: string = "Pakistan";
const fullAddress: string = `${city}, ${country}`;
console.log(`Address: ${fullAddress}`);

// Today's temperature with number type annotation
let temperature: number = 25;  // Adjust this to the actual temperature
console.log(temperature)